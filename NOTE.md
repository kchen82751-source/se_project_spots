Select all the modals using the method querySelectorAll

```js
const modals = document.querSelectorAll(".modal");
```

Iterate through each modal

```js

modals.forEach((modal)) => {
/* Determine if where you have clicked
is it the modal or the modal__container
if it is the modal close the modal
if it is not the modal do not close the modal
*/

/*
Add an event listener for a keyup event
it the event key === Escape
close the modal
*/
}
```
